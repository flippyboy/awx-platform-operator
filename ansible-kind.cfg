# ansible-core 2.16+ requires boolean `when:` results. Upstream installer still
# uses patterns like `when: foo | length` (returns int). Allow for kind
# standalone reconciles until upstream is updated.
[defaults]
allow_broken_conditionals = True
retry_files_enabled = False
stdout_callback = default
interpreter_python = auto_silent
