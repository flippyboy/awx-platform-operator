apiVersion: v1
kind: Service
metadata:
  name: jewel-nodeport
  namespace: awx-platform
spec:
  type: NodePort
  selector:
    app: jewel
  ports:
    - name: https
      port: 8000
      targetPort: 8000
      nodePort: 30443
